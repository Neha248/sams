# Vite dev server proxies /api to this URL (must match backend PORT in backend/.env).
# Local npm dev: 5001. Docker full stack backend: 5000 (use only if not running npm backend).
VITE_API_PROXY_TARGET=http://localhost:5001
